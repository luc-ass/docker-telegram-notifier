{
  "name": "docker-telegram-notifier",
  "version": "1.6.0-beta.1",
  "description": "Docker container to inform you about docker-events via telegram",
  "repository": {
    "type": "git",
    "url": "https://github.com/luc-ass/docker-telegram-notifier"
  },
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "healthcheck": "node app.js healthcheck",
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=lcov.info"
  },
  "author": "poma, arefaslani",
  "license": "Apache-2.0",
  "dependencies": {
    "dockerode": "^5.0.1",
    "https-proxy-agent": "^7.0.6",
    "JSONStream": "^1.3.5",
    "telegraf": "^4.16.3"
  }
}
